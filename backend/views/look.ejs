<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Look</title>
  </head>
  <body>
    
    <div class="container h-screen w-screen flex flex-col items-center justify-center bg-slate-400">
        <h1
        class="text-3xl fixed top-8 cursor-pointer font-semibold text-white"
      >
        <a href="/">Home</a>
      </h1>
        <div class="content ">
            <div class="card rounded-xl overflow-hidden bg-slate-600">
                <div class="card-body px-8 py-4 flex flex-col items-center text-white">
                    <p class="text-2xl">Name: <%=item.name%></p>
                    <p class="text-2xl">Price: $<%=item.price%></p>
                    <p class="text-2xl">Description: <%=item.description%></p>
                    <p class="text-2xl mt-4">Image:</p>
                </div>
                <img width="700px" src="/images/uploads/<%=item.image%>" alt="${item.name}" />
        </div>
    </div>
  </body>
</html>
